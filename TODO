field automatically free will be prohibited


vector



clone

cast

convert struct name wchat_, wchar_t*

clone

output to the debug code when creating generics functions.

check automatically free especially right value heap free and break and return

array 
global variable 
char* aaa[3] = { "ABC", "A", "C" };
char* aaa[2][2] = { "AA", "BB", "CC", "DD" };

-----
finalize
a++
+=
a--
-=
inline with heap
co-routine with type

coroutine
struct, union, 
load store field
run co-routine
subroutine header only all function is inline or generics
generics
compile generics function when calling function
method generics
mixin-layers of function
inherit void fun(void);
enum
anonymous union
anonymous struct

strcut Data {
    int a:1;
    int b:2;
};
,
++a
--a

*=
/=
%=
&=
^=
|=
<<=
>>=

array []

array initializer
'a'
'\0'

operators

for
while
do, while
switch
loop break automatically free
loop break
loop continue

int a[3][3] = { { 1,2,3 }, { 1,2,3 }, {1,2,3}};
global variable multiple array access store
cast

sizeof
debug info
precompile すべての演算子のcompileをprecompileで通さないといけない。すべてのコンパイルでprecompile時もCoroutineとStoreの検出。
それかblockはパース時にvar_tableを作りパース時に変数を作成する。そしてパース時にコルーチンのコンパイルを行うか。
include stdio.h
#include <stdio.h>
preprocesser

__ASM_NAME__ など
